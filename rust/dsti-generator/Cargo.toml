[package]
name = "docspec-dsti-generator-rust"
version = "0.1.0"
edition = "2021"
description = "DSTI test generator for Rust (#[test] + proptest)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
