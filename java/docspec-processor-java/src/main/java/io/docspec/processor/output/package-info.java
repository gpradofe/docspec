@DocModule(
    id = "docspec-output",
    name = "DocSpec Output",
    description = "JSON serialization layer using Jackson ObjectMapper to produce docspec.json output.",
    since = "3.0.0",
    audience = "internal"
)
package io.docspec.processor.output;

import io.docspec.annotation.DocModule;
