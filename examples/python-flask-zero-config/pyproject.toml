[project]
name = "python-flask-zero-config"
version = "1.0.0"
description = "Tier 0: Flask app with zero DocSpec annotations"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
