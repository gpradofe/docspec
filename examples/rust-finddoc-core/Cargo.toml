[package]
name = "finddoc-core"
version = "1.0.0"
edition = "2021"
description = "DocSpec Rust Example — FindDoc Document Processing Core"

[dependencies]
docspec-macros = { path = "../../annotations/rust" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
