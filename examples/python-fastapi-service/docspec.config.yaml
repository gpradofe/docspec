# DocSpec configuration for Data Sync Service
artifact:
  name: data-sync-service
  label: Sync Service
  color: "#10B981"
  version: "1.0.0"

source:
  type: local
  path: ./src
  language: python
  framework: fastapi

output:
  path: ./docspec-output
  format: json
  intentGraph: true

documentation:
  tier: 2
  includePrivate: false
