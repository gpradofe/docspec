[package]
name = "docspec-rust-minimal"
version = "1.0.0"
edition = "2021"
description = "Tier 1 DocSpec example — minimal annotations in Rust"

[dependencies]
docspec-macros = { path = "../../annotations/rust" }
