package com.waypoint.agent;

/**
 * Validates and normalizes goal specifications.
 *
 * <p>Ensures that the raw {@link GoalSpec} provided by the user meets
 * minimum validity requirements before passing it downstream to the
 * skill decomposition step.</p>
 */
public class GoalParser {

    /**
     * Parses a raw goal spec into a validated ParsedGoal.
     *
     * @param goal the raw goal specification from the user
     * @return a validated and normalized parsed goal
     */
    public ParsedGoal parse(GoalSpec goal) {
        return null;
    }
}
