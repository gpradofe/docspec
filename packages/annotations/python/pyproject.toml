[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "docspec-py"
version = "0.1.0"
description = "DocSpec decorators for Python"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "mypy>=1.0"]
