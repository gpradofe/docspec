[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "docspec-dsti-generator-python"
version = "0.1.0"
description = "DSTI test generator for Python (pytest + Hypothesis)"
requires-python = ">=3.10"
license = "Apache-2.0"
